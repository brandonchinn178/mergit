defaults:
  local: ../hpack.yaml

name: merge-bot-core
version: 0.1.0

dependencies:
  - aeson
  - base
  - bytestring
  - containers
  - graphql-client
  - http-client
  - http-types
  - iso8601-time
  - text
  - time
  - transformers

library:
  source-dirs: src
