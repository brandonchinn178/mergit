defaults:
  local: ../hpack.yaml

name: merge-bot-core
version: 0.1.0

dependencies:
  - aeson
  - base
  - bytestring
  - containers
  - exceptions
  - github
  - http-client
  - http-client-tls
  - http-types
  - mtl
  - text
  - vector

library:
  source-dirs: src
