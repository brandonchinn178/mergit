defaults:
  local: ../hpack.yaml

name: merge-bot
version: 0.1.0

dependencies:
- aeson
- base
- bytestring
- case-insensitive
- cryptonite
- merge-bot-core
- mtl
- text
- wai
- x509
- x509-store

library:
  source-dirs: src

executables:
  merge-bot:
    source-dirs:  exe
    main:         Main.hs
    dependencies:
    - merge-bot
    - warp
