defaults:
  local: ../hpack.yaml

name: graphql-client
version: 0.1.0

dependencies:
  - aeson
  - aeson-schemas
  - base
  - bytestring
  - exceptions
  - http-client
  - http-client-tls
  - http-types
  - monad-control
  - mtl
  - path
  - path-io
  - scientific
  - template-haskell
  - text
  - transformers
  - transformers-base
  - unordered-containers

library:
  source-dirs: src

tests:
  graphql-client-test:
    source-dirs:  test
    main:         Main.hs
    dependencies:
      - graphql-client
      - tasty
      - tasty-golden
